{% import "_includes/forms" as forms %}

{{ forms.lightswitchField({
    first: true,
    label: "Inject CORS tokens using AJAX",
    instructions: "Allows CORS tokens to be used on statically cached pages. This can collide with similar functionality in other plugins.",
    id: 'injectCors',
    name: 'injectCors',
    value: settings.injectCors
}) }}
