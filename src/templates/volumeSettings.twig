{% import "_includes/forms" as forms %}

{{ forms.autosuggestField({
    label: "Subfolder"|t('servd-asset-storage'),
    instructions: "The plugin will automatically differentiate between local, stage and production data, but if you want to use a further subfolder, specify the path to use here."|t('servd-asset-storage'),
    id: 'customSubfolder',
    class: 'ltr',
    name: 'customSubfolder',
    suggestEnvVars: true,
    value: volume.customSubfolder,
    errors: volume.getErrors('customSubfolder'),
    placeholder: "path/"|t('servd-asset-storage')
}) }}

{{ forms.autosuggestField({
    label: "Optimise Prefix"|t('servd-asset-storage'),
    id: 'optimisePrefix',
    name: 'optimisePrefix',
    suggestEnvVars: true,
    value: volume.optimisePrefix,
    errors: volume.getErrors('optimisePrefix'),
    class: '',
    instructions: 'A custom domain to use for optimised images. Only set this if you have configured a proxy which will forward requests to Servds optimisation service domain. <a target="_blank" ref="noopener" href="https://servd.host/docs/can-i-use-my-own-domain-for-assets">Read More</a>',
    placeholder: "https://images.yourdomain.com"

}) }}
